# scripts/ enthält CLI-/Cron-Skripte – dürfen nicht per Web-Request ausgelöst werden.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
