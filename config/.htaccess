# config/ enthält nur PHP-Includes und Zugangsdaten – niemals direkt per URL abrufbar.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
